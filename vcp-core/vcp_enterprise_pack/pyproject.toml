[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "vcp-sdk"
version = "2.0.0"
description = "Verifiable Delegation & Execution Authority Middleware for AI Agents"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "pyyaml>=6.0",
    "requests>=2.28.0"
]
